name: Sync runtime local fallback files
on:
  pull_request:
  schedule:
    # https://crontab.guru/every-night-at-midnight
    - cron: "0 0 * * *"

jobs:
  sync-local-fallback-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          extensions: dom, iconv, json, libxml, zip
          tools: composer

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "::set-output name=dir::$(composer config cache-files-dir)"

      - name: Configure Composer cache
        uses: actions/cache@v2
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install Composer dependencies
        run: composer install

      - name: Get current date
        id: date
        run: |
          date +'%Y-%m-%d'
          echo "::set-output name=today::$(date +'%Y-%m-%d')"

      - name: Sync local fallback files
        run: php bin/sync-amp-runtime-local-fallback-resources.php

      - name: Get pull request body text
        id: body
        run: |
          BODY=$(bin/render-pr-body.bash)
          echo $BODY
          BODY="${BODY//$'\n'/'%0A'}"
          echo "::set-output name=text::$BODY"

      - name: Commit and push changes
        uses: github-actions-x/commit@v2.7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          push-branch: "sync-local-fallback-files"
          commit-message: "Sync local fallback files - ${{ steps.date.outputs.today }}"
          name: Alain Schlesser
          email: alain.schlesser@gmail.com

      - name: Create or update pull request
        uses: gr2m/create-or-update-pull-request-action@v1
        with:
          author: "Alain Schlesser <alain.schlesser@gmail.com>"
          branch: "sync-local-fallback-files"
          body: "${{ steps.body.outputs.text }}"
          title: "Sync local fallback files"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
